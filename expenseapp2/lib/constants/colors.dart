import 'package:flutter/material.dart';

// Ana renkler
const Color primaryColor = Color(0xFF4A90E2); // Mavi tonu
const Color accentColor = Color(0xFF50C878); // Yeşil tonu
const Color errorColor = Color(0xFFE57373); // Hata için kırmızı tonu

// AppBar için renkler
const Color appBarBackgroundColor = primaryColor; // Mavi tonu
const Color appBarForegroundColor = Colors.white; // Beyaz

// Card için renkler
const Color cardBackgroundColor = Colors.white; // Beyaz

// Daire ve ikon için renkler
const Color circleColor = Color(0xFFDAA520); // Altın tonu
const Color iconColor = Colors.black; // Siyah

// Başlık ve alt başlık renkleri
const Color expenseItemTitleColor = Colors.blueGrey; // Mavi-Gri
const Color expenseItemSubtitleColor = Colors.black; // Siyah
